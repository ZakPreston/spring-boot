
  - name: Kill existing spring-boot-sample-web-ui-latest.jar
    shell: pkill -f *spring-boot-sample-web-ui* 2> /dev/null
    ignore_errors: yes

  - maven_artifact:
      version: 12
      group_id: org.springframework.boot
      artifact_id: spring-boot-sample-web-ui
      repository_url: 'http://192.168.110.100:8081/repository/Spring2020/'
      #      username: "{{nexus_user}}"
      #      password: "{{nexus_password}}"
      dest: ~/spring-boot-sample-web-ui-latest.jar
#      keep_name: yes
 
  - name: Deploy Artifact
    shell: nohup java -jar ~/spring-boot-sample-web-ui-latest.jar --server.port={{artifact_port}} &
